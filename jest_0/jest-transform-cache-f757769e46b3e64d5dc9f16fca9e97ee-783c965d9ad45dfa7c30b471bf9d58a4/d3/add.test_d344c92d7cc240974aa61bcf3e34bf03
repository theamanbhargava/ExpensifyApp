'use strict';var add = function add(a, b) {return a + b;};

test('should add two numbers', function () {
    var result = add(4, 4);
    if (result !== 7) {
        throw new Error('You added 4 and 3, expected value was 7, got ' + result);
    }
});